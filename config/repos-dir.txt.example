# Path to the directory containing your git repos (one path per line)
# The first non-comment, non-empty line is used as the repos folder
# Use ~ for home directory (it will be expanded automatically)
#
# Example:
# ~/repos/tsilva
